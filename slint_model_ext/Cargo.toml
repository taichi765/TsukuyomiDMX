[package]
name = "slint_model_ext"
edition = "2024"
version.workspace = true

[dependencies]
i-slint-core = { path = "../../slint/internal/core" }
slint = { workspace = true }

derive_more = { workspace = true, features = ["debug"] }
