export enum EffectKind{
    Simple,
    Sequence,
    Parallel,
    SimpleSpec,
    SequenceSpec,
    ParallelSpec,
    SimpleTemplate,
    SequenceTemplate,
    ParallelTemplate,
}

export enum ChannelKind { 
    Dimmer,
    Red,
    Green,
    Blue,
}